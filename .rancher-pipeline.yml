stages:
- name: Build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: library/app:v1
      pushRemote: true
      registry: harbor:30003
timeout: 60
notification: {}
