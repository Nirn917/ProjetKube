stages:
- name: Build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: library/app:v1
      pushRemote: true
      registry: 127.0.0.1:34856
timeout: 60
notification: {}
